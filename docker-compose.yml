version: "3"
services:
  stock-app:
    build: .
    container_name: stock-app
    ports:
      - "9009:9009"
    volumes:
      - .:/app
    environment:
      - FLASK_APP=main.py
      - FLASK_ENV=production
    restart: always
